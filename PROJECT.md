# PROJECT — Conductor Index

> GENERATED by the `pm` plugin — do not hand-edit. Source of truth is
> `.conductor/state.json` (ordering, detours, links) + OpenSpec `tasks.md` (stories).
> Regenerate: `/pm:status` (or `node scripts/conductor.mjs render`).
> Last rendered: 2026-07-11T00:26:43.233Z

## Now

**`native-tools-support`** — Native tool support (web_search, file_search, code_interpreter) via Responses API (epic, P3) — —

## Detour stack

_Empty — no work is paused._

## Epics

| Priority | Epic | Lane | Role | Status | Progress | Links |
|----------|------|------|------|--------|----------|-------|
| P0 | `openai-sdk-modernization` | claude-code | epic | archived | — | - |
| P1 | `conversations-api-support` | openspec | epic | archived | — | depends-on→openai-sdk-modernization; relates-to→responses-api-chaining |
| P2 | `responses-api-chaining` | openspec | epic | archived | — | depends-on→openai-sdk-modernization; relates-to→conversations-api-support |
| P2 | `multimodal-input` | superpowers | epic | archived | — | depends-on→openai-sdk-modernization |
| P2 | `structured-outputs` | superpowers | epic | archived | — | depends-on→openai-sdk-modernization |
| P3 | `native-tools-support` | openspec | epic | active ⚠ no change on disk | — | depends-on→openai-sdk-modernization; depends-on→responses-api-chaining |
| P3 | `streaming-progress-notifications` | openspec | epic | queued ⚠ no change on disk | — | depends-on→openai-sdk-modernization |
| P3 | `retry-backoff-config` | claude-code | epic | archived | — | depends-on→openai-sdk-modernization |

## Recent detours

_None logged._

## Briefing (what a fresh session sees)

```
CONDUCTOR STATE — where we are and what's next

NOW: `native-tools-support` (openspec, epic, P3) — —

EPIC LINKS:
  • `conversations-api-support` depends-on `openai-sdk-modernization` — needs the responses/conversations resources added in openai v6
  • `conversations-api-support` relates-to `responses-api-chaining` — alternative lighter-weight approach to the same problem; pick one or both
  • `responses-api-chaining` depends-on `openai-sdk-modernization` — needs the responses resource added in openai v6
  • `responses-api-chaining` relates-to `conversations-api-support` — alternative lighter-weight approach to the same problem; pick one or both
  • `multimodal-input` depends-on `openai-sdk-modernization` — widen message content types on the upgraded SDK
  • `structured-outputs` depends-on `openai-sdk-modernization` — uses text.format / response_format on the upgraded SDK
  • `native-tools-support` depends-on `openai-sdk-modernization` — needs the responses resource
  • `native-tools-support` depends-on `responses-api-chaining` — builds on the Responses API integration
  • `streaming-progress-notifications` depends-on `openai-sdk-modernization` — needs streaming support from the upgraded SDK
  • `retry-backoff-config` depends-on `openai-sdk-modernization` — relies on v6's improved retry/backoff handling

RULES (pm): classify detours before fixing — minimal → fix+commit then `/pm:detour --minimal`; substantial → `/pm:detour` (own proposal + PUSH). After any state change, `/pm:status`. Resume via `/pm:resume` + reconcile gate. Mirror every PUSH/POP to a one-line Honcho memory.

Manage with /pm:status · /pm:next · /pm:detour · /pm:resume, or the `conductor` skill.
```
